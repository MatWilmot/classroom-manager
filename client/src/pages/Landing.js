import React, { useEffect } from "react";
import M from "materialize-css/dist/js/materialize.min.js";

const Landing = () => {
  // If I need use use an initialized materialize component
  // useEffect(() => {
  //   M.AutoInit();
  // }, []);

  return (
    <div>
      <h1>Landing Page</h1>
    </div>
  );
};

export default Landing;
